import React from 'react';

const ResumeSheet = React.forwardRef((props, ref) => {
  return (
    <div
      style={{
        boxShadow: "rgb(60 64 67 / 15%) 0px 1px 3px 1px",
        width: "40vw",
        height: "90vh",
        backgroundColor: "#fff",
      }}
      ref = {ref}
    > DC,DFD</div>
  );
});

export default ResumeSheet;

